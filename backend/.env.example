# SML Takeoff Backend (ONLINE-ready)
# Local
PORT=10000
NODE_ENV=development

# Security
JWT_SECRET=change_me_super_secret

# CORS
# Comma-separated list of allowed origins (no trailing slash)
# Example:
# CORS_ORIGINS=http://localhost:5173,https://your-vercel-app.vercel.app
CORS_ORIGINS=http://localhost:5173

# Storage mode:
# local = store uploads in backend/uploads
# r2    = store uploads in Cloudflare R2 (S3 compatible)
STORAGE_MODE=local

# R2 / S3 (only used when STORAGE_MODE=r2)
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=
S3_PUBLIC_BASE_URL=
